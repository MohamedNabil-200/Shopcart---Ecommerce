import { Search } from "lucide-react";

const SearchBar = () => {
  return (
    <div>
      <Search className="w-5 h-5 cursor-pointer hover:text-shop-light-green hoverEffect" />
    </div>
  );
};

export default SearchBar;
